import 'package:flutter/material.dart';

import 'components/body.dart';

class QuestionsChapter2 extends StatelessWidget {
  static String routeName = "/questionsChapter-02";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Body(),
      ),
    );
  }
}
